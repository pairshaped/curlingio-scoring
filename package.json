{
  "scripts": {
    "start": "nf start",
    "client": "elm-live src/Scoring.elm --start-page=dev.html --open -- --output=dev.js",
    "server": "json-server -w db.json"
  },
  "dependencies": {
    "elm": "^0.19.1-no-deps",
    "elm-live": "^4.0.0",
    "foreman": "^3.0.1",
    "json-server": "^0.15.1",
    "uglify-js": "^3.6.0"
  }
}
