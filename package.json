{
  "scripts": {
    "start": "nf start",
    "client": "elm-live src/Main.elm --start-page=dev.html --open -- --output=dev.js",
    "server": "json-server --watch db.json"
  },
  "dependencies": {
    "elm": "^0.19.0-no-deps",
    "elm-live": "^4.0.0",
    "foreman": "^3.0.1",
    "json-server": "^0.15.1",
    "uglify-js": "^3.6.0"
  }
}
